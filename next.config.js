/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["via.placeholder.com", "api.slingacademy.com", "images.pexels.com"],
    }
}

module.exports = nextConfig
